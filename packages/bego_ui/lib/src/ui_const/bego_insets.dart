import 'package:flutter/material.dart';

final class BegoInsets {
  const BegoInsets._();

  static const textSpaceing = 4.0;
  static const inset2Vertical = EdgeInsets.symmetric(vertical: 2);
  static const inset2 = EdgeInsets.all(2);
}
